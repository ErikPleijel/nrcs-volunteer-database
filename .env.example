APP_NAME="Red Cross Volunteers"
# -> APP_ENV=production
APP_ENV=local
# Generate, put copy in safe place
APP_KEY=base64:lf6X61aTLYB+cT8RPx6JfN/DGj+T4I3jaaSspFPqpes=
# -> https://nrcsvdbm.org
APP_URL=https://dev.itacenmu.org
# -> False
APP_DEBUG=true
# Verify
OFFICIAL_WEBSITE_URL=https://redcrossnigeria.org

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

# Optional display-only override for the campaign email footer's database link
# (e.g. once NRCS sets up a redirect from their main domain, like redcrossnigeria.org/database). Falls back to the
# app's real URL (APP_URL, via route('welcome')) when unset.
CAMPAIGN_DISPLAY_URL=https://dev.itacenmu.org/database

# set date = today
NRCS_DB_MIGRATION_DATE=2026-07-19
CAMPAIGN_FROM_NAME="Nigerian Red Cross Society"
# make sure no-reply email exists.
CAMPAIGN_REPLY_TO_EMAIL=no-reply@nrcs.org
# Set
SUPER_ADMIN_EMAILS="secretary.general@example.com,erikpleijel@yahoo.se,user2@example.com"

LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
# set to LOG_LEVEL=warning in production
LOG_LEVEL=debug
LOG_CAMPAIGN_DELIVERY_LEVEL=info
LOG_CAMPAIGN_DELIVERY_DAYS=30
LOG_SCHEDULER_LEVEL=info
LOG_SCHEDULER_DAYS=14

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=redcross_volunteer_new
DB_USERNAME=redcross_user
DB_PASSWORD=bhu1nji2mko3db$

# Filesystem path to the old image directory, OR an http(s) URL prefix. Used by
# `php artisan images:migrate` for one-time legacy photo/signature migration.
IMAGE_MIGRATION_SOURCE=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null

# Set this, default mail reply
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
