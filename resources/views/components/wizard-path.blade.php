@props(['wizardName'])

<p class="text-sm text-gray-600">
    Use <span class="font-semibold">Persons → Campaign filter wizard  → <u>{{ $wizardName }}</u></span>
    to build the list → Optional: Choose segments: Area · Gender · Age →
    <span class="font-semibold">Make campaign from filter</span> and follow the wizard.
</p>
